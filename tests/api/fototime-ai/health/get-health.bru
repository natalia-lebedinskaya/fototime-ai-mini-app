meta {
  name: GET health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/health
  body: none
  auth: none
}

tests {
  test("status code is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("service status is ok", function() {
    expect(res.body.status).to.equal("ok");
  });
}
